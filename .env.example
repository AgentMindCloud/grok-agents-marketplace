# Optional — a GitHub personal access token raises the anonymous
# 60 req/hr rate limit to 5000 req/hr. No scopes required.
# https://github.com/settings/tokens
GITHUB_TOKEN=

# Base URL for absolute metadata (OG images, canonical URLs). Optional.
NEXT_PUBLIC_SITE_URL=https://grokagents.dev
